extends ../../layouts/signMain.pug

include ../../mixins/musicBlock
block content
    .musicDetail__container
        +musicBlock({
            image: musics.image,
            fileUrl: musics.fileUrl,
            title: musics.title,
            genre: musics.genre,
            description: musics.description,
            views: musics.views,
            artist: musics.artist,
        })
        if loggedUser && (loggedUser.dataValues.email === "fhghzl112@naver.com")
            a(href=routes.musicDelete(musics.id))
                button Delete